export const staticRoutes = {
    // public routes
    home: '/',
    active: '/active/:activeId',
    notification: '/notification/:notificationId',
    settings: '/settings',
    // protected routes
} as const;
