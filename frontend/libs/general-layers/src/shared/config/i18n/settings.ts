const defaultLocale = 'en';
const locales = [defaultLocale, 'ru'];
const defaultNamespace = 'translation';
const namespaces = [defaultNamespace, 'home-page'];

export const i18nConfig = {
    defaultLocale,
    defaultNamespace,
    locales,
    namespaces,
    prefixDefault: false,
};
