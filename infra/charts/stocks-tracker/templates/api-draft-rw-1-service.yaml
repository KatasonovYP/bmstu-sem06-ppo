apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.meta.name }}-api-draft-rw-1-service
  namespace: {{ .Values.meta.namespace }}
spec:
  selector:
    app: {{ .Values.meta.name }}-api-draft-rw-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.port }}
