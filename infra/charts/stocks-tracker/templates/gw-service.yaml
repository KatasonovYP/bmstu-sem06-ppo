apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.meta.name }}-gw-service
  namespace: {{ .Values.meta.namespace }}
spec:
  selector:
    app: {{ .Values.meta.name }}-gw
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.gw.port }}
