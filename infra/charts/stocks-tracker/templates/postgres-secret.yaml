apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.meta.name }}-postgres-secret
  namespace: {{ .Values.meta.namespace }}
type: Opaque
data:
  postgres-password: {{ .Values.postgres.password | b64enc | quote }}
