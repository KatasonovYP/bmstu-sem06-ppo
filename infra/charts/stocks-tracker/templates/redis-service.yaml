apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.meta.name }}-redis-service
  namespace: {{ .Values.meta.namespace }}
spec:
  selector:
    app: {{ .Values.meta.name }}-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
