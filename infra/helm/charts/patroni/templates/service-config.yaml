apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.meta.name }}-patroni-config
  labels:
    application: {{ .Values.meta.name }}-patroni
    cluster-name: {{ .Values.meta.name }}-patroni
spec:
  clusterIP: None
