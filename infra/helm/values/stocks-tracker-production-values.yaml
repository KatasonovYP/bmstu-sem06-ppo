meta:
  name: stocks-tracker-production

image:
  tag: '{{ env "CI_COMMIT_SHORT_SHA" }}'

ingress:
  api:
    host: api.stocks-tracker.ru
  admin:
    host: admin.stocks-tracker.ru

env:
    APP_POSTGRES_USER: postgres
    APP_POSTGRES_HOST: stocks-tracker-production-postgres-service.stocks-tracker.svc
    APP_POSTGRES_PORT: '5432'
    APP_POSTGRES_DATABASE: postgres
    APP_MONGO_USER: ""
    APP_MONGO_PASSWORD: ""
    APP_MONGO_HOST: stocks-tracker-production-mongodb-service
    APP_MONGO_PORT: "27017"
    APP_MONGO_DATABASE: stocks_tracker
    APP_USE_MONGO: "true"

